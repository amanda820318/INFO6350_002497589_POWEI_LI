name: hackathon
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # 🔥 Firebase core (required)
  firebase_core: ^3.6.0

  # 🔥 Firebase Auth (Google Sign-In 需要)
  firebase_auth: ^5.3.1

  # 🔥 Google Sign-In
  google_sign_in: ^6.2.1

  # 🔥 Firestore (讀寫 quiz 題目 + 儲存分數)
  cloud_firestore: ^5.4.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
